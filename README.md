# SEO Analyzer Service

Сервис для анализа HTML-страниц: извлекает `title`, `h1` и кэширует результаты с TTL.

---

## 📁 Структура проекта

```
seo-analyzer/
├── Dockerfile               # Основной Dockerfile проекта
├── requirements.txt         # Список Python-зависимостей
├── .gitignore               # Исключения для Git
├── README.md                # Описание проекта (этот файл)
└── app/
    ├── main.py              # Точка входа (FastAPI-приложение)
    ├── models.py            # Pydantic-модели для валидации данных
    ├── parser.py            # Логика парсинга HTML (извлечение title/h1)
    └── cache.py             # Кэширование результатов с TTL
```

## 📌 Endpoints

### 1. POST /api/analyze
**Описание:** Анализирует SEO-метаданные страницы

**Запрос:**
```json
{
  "url": "https://example.com"
}
```

**Ответ:**
```json
{
  "url": "https://example.com",
  "title": "Example Domain",
  "h1_count": 1,
  "meta_description": "This domain is for examples.",
  "cached": false
}
```

---

### 2. POST /api/getContactOnSite
**Описание:** Ищет email и телефоны на сайте

**Запрос:**
```json
{
  "url": "example.com"
}
```

**Ответ:**
```json
{
  "url": "https://example.com/contacts",
  "emails": ["info@example.com", "support@example.com"],
  "phones": ["+7 (999) 123-45-67", "+7 (495) 000-00-00"],
  "found_on_main": false
}
```

---

### 3. DELETE /api/cache
**Описание:** Очищает кеш

**Запрос:** Без параметров

**Ответ:**
```json
{
  "message": "Кеш очищен"
}
```

---

## ▶️ Запуск локально

Убедитесь, что у вас установлен Python 3.12+.

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

---

## 🐳 Запуск в Docker

```bash
# Сборка образа
docker build -t seo-analyzer .

# Запуск контейнера
docker run -p 8000:8000 seo-analyzer
```

После запуска сервис будет доступен по адресу: [http://localhost:8000](http://localhost:8000)

---

## 🧪 Тестирование

```bash
# Установка зависимостей (если ещё не установили)
pip install -r requirements.txt

# Запуск тестов
pytest
```

Тесты полностью изолированы от интернета: используют моки для HTTP-запросов.

---

## ✅ Чек-лист перед отправкой

- [x] Парсер корректно извлекает `title` и `h1`.
- [x] Сервис не падает при ошибке соединения с целевым сайтом.
- [x] В тестах используются моки (работают без интернета).
- [x] `Dockerfile` включает установку всех зависимостей.

